{
  "name": "signex",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "start": "turbo run start",
    "test": "turbo run test",
    "db:generate": "turbo run generate --filter=@signex/db",
    "db:migrate": "npm run migrate --workspace @signex/db",
    "test:acceptance": "bash test/acceptance.sh",
    "test:invariants": "npm run test:e2e -w @signex/api -- invariants",
    "test:all": "npm run build && npm run test -w @signex/api && npm run test:invariants",
    "test:docker-build": "docker compose build"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "npm@10.9.0",
  "engines": {
    "node": ">=18"
  }
}
